import axios from "axios";

//액션 타입

//액션 생성자 함수

//초기 상태
const initialState = {};

//리듀서
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
